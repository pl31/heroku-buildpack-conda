#!/usr/bin/env bash

if [ -e ./app/additional_notebook_config.py ]
then
    with_config="--config=./app/additional_notebook_config.py"
fi

echo "
---
addons: ~
config_vars: ~
default_process_types:
  web: .conda/bin/ipython notebook --no-browser --ip=* --port \$PORT $with_config
"
